apiVersion: 1

datasources:
  - name: TimescaleDB
    type: postgres
    access: proxy
    url: timescaledb:5432
    database: telemetry
    user: astro
    jsonData:
      sslmode: disable
      timescaledb: true
    secureJsonData:
      password: astro
    isDefault: true
    editable: false

  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9091
    isDefault: false
    editable: false
